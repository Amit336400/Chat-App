package com.example.mychat.Problrm_Error

/* TODO
when one user create a channel this display both user home screen
but the problem is another user if click same user then create a new channel


 */

/**
 * I can not understand
 * in new chant view Model code and
 */

/**
 *  launch {
 *                 repo.getChannelWithFlowMessage(channelId).collectLatest {
 *                     channel.update(repo.getChannel(channelId))
 *                 }
 *             }
 *
 *             i can not understand why get channel call two time ??
 */